server:
  port: 9090
app:
  messages:
    enroll: 
    verify:
spring:
  datasource:
    username: system
    password: root
    url: jdbc:oracle:thin:@localhost:1521:xe
    driver-class-name: oracle.jdbc.driver.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true